{
  "name": "urspace-bootstrap-worker",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash ../../scripts/build-bootstrap.sh",
    "test": "node --test",
    "check": "npm run build && npm test && node --check src/index.js && WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run --outdir .wrangler-dist",
    "deploy": "npm run build && WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
